document.addEventListener('DOMContentLoaded', function() {
    // Funciones relacionadas con el parking
    // Por ejemplo, registrar la entrada de un vehículo
    document.getElementById('formRegistrarEntrada').addEventListener('submit', function(event) {
        event.preventDefault(); // Evitar el envío normal del formulario
        // Lógica para registrar entrada de vehículo
    });

    // Otras funciones relacionadas con la gestión del parking
});